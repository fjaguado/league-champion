class Player {
    constructor(id, name, team, score, createdAt, updatedAt) {
        this.id = id;
        this.name = name;
        this.team = team;
        this.score = score;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }
}

export default Player;
